server {
       listen {{ nginx_port }};
       listen [::]:{{ nginx_port }};

       server_name {{ nginx_server_name }};

       root {{ nginx_dir }};
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}